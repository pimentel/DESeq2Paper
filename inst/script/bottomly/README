Explanation of files in /inst/script/bottomly:

calls              : the tophat2 alignment call for the fastq files from SRA
countReads.R       : counts the reads for the count-based methods
diffExpr.R         : runs the count-based methods and reads in cuffdiff2 results
getPheno.R         : reads the phenotypic information from SRA and creates random subsets
random_calls.txt   : the cuffdiff2 calls
random_subsets.txt : the list of samples for each random replicate, first evaluation (3 vs 3)
                   : then verification (7 vs 8)
